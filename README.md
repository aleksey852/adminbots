# Admin Bots Platform - Multi-Bot Management 🚀

Единая платформа для управления множеством Telegram ботов через одну админ-панель. Идеально подходит для проведения чековых промо-акций, розыгрышей и рассылок.

## ✨ NEW in v2.3: Import/Export & Custom Bots

**📦 Полный бэкап бота:**
- ZIP-архив со всеми настройками, пользователями и данными.
- Импорт бота из архива с новым токеном — мгновенный клон.

**🤖 Кастомные боты:**
- Папка `bots/` для размещения собственных модулей.
- Документация с ТЗ для разработки.

**⚡ Быстрое создание:**
- Чекбоксы для ежемесячных розыгрышей и требования подписки.

---

## 🚀 Быстрый старт

```bash
# Клонируйте репозиторий
git clone https://github.com/aleksey852/adminbots.git admin-bots-platform
cd admin-bots-platform

# Запустите установку (Zero-Config)
sudo bash scripts/deploy.sh
```

Скрипт **полностью автоматический**:
1. Сам сгенерирует пароли и настроит базу данных.
2. Настроит доступ по IP адресу сервера.
3. В конце выдаст **логин и пароль** от админ-панели.

Все настройки ботов и токенов вы сделаете уже внутри красивого веб-интерфейса.

### 🔄 Обновление

```bash
sudo bash /opt/admin-bots-platform/scripts/update.sh
```

Скрипт **автоматически**:
1. 📥 Скачает новую версию кода (git pull).
2. 📦 Обновит зависимости (pip install).
3. ♻️ Перезапустит сервисы.
4. ✅ Проверит статус работы.

## 💻 Архитектура

Платформа состоит из единого веб-интерфейса и менеджера ботов, который запускает экземпляры `aiogram` для каждого подключенного токена.

**Структура процессов:**
```
Admin Panel (FastAPI)      Bot Manager (Process)
      │                            │
      │   [Switch Context]         │ 1. Load Active Bots
      │                            ├────> Bot 1 (Token A)
      │                            ├────> Bot 2 (Token B)
      │                            └────> ...
      │                                    │
      │      PostgreSQL DB                 │
      │  ┌─────────────────────┐           │
      ├──>| bots (id, token)    |<──────────┤
      │  | users (bot_id, ...) |           │
      │  | campaigns (bot_id)  |           │
      │  └──────────┬──────────┘           │
      │             │ NOTIFY               │
      │             └──────────────────────> 2. LISTEN & ACT
```

## 📁 Структура проекта

```
admin-bots-platform/
├── main.py              # Entry point: запускает Bot Manager и Scheduler
├── bot_manager.py       # Управление процессами ботов (Start/Stop)
├── config.py            # Глобальная конфигурация (DB, Redis, Auth)
├── database/
│   ├── bot_db.py       # Менеджер подключений к БД ботов
│   ├── panel_db.py     # БД панели (реестр ботов, пользователи)
│   └── bot_methods.py  # Изолированные методы работы с БД
├── modules/            # Модульная структура
│   ├── core/           # Базовый функционал (меню, профиль, FAQ)
│   ├── registration/   # Регистрация (с настройкой подписки)
│   ├── admin/          # Инструменты администратора
│   ├── promo/          # Активация промокодов (promo-боты)
│   ├── receipts/       # Загрузка чеков (receipt-боты)
│   └── raffle/         # Розыгрыши (с/без промежуточных)
├── bots/               # Кастомные боты (см. bots/README.md)
├── admin_panel/
│   ├── app.py          # Веб-панель + Middleware для bot_id
│   └── routers/        # Маршруты API (Bots, Users, System)
└── scripts/
    ├── deploy.sh       # Zero-Config установка
    └── update.sh       # Авто-обновление
```

## 🧩 Типы ботов и модулей

### Типы ботов
| Тип | Описание | Автоматически включённые модули |
|-----|----------|--------------------------------|
| **Receipt** | Чековое промо | `core`, `registration`, `receipts`, `raffle`, `admin` |
| **Promo** | Промокоды | `core`, `registration`, `promo`, `raffle`, `admin` |

### Настраиваемые опции

| Модуль | Настройка | Описание |
|--------|-----------|----------|
| **registration** | `subscription_required` | Требовать подписку на канал |
| **registration** | `subscription_channel_id` | ID канала для подписки |
| **raffle** | `intermediate_enabled` | Промежуточные розыгрыши |
| **raffle** | `intermediate_period` | Период (daily/weekly/monthly) |


## 🌐 Веб-панель

Доступна по адресу: `https://your-domain.com`

### 🤖 Управление ботами

| Действие | Описание |
|----------|----------|
| **Добавить** | `/bots/new` — токен, имя + чекбоксы опций |
| **Импорт** | `/bots/import` — загрузить ZIP-архив с данными |
| **Экспорт** | Полный архив (настройки + данные) из профиля бота |
| **Переключение** | Выпадающее меню в боковом меню |

### Основные функции (для активного бота)

| Раздел | Описание |
|--------|----------|
| **Dashboard** | Статистика, графики и быстрые действия для текущего бота. |
| **Рассылка** | Создание рассылок (текст+фото). Мгновенный запуск. |
| **Розыгрыш** | Проведение розыгрышей призов среди участников. |
| **Пользователи** | CRM система: просмотр профиля, чеков, блокировка, личные сообщения. |
| **Чеки** | Модерация и просмотр всех загруженных чеков. |
| **Модули** | Включение/выключение и настройка модулей бота. |

### ⚙️ Динамические настройки
Каждый бот настраивается индивидуально через UI, без редактирования файлов:
- **Промо**: Название акции, сроки, призы.
- **Тексты**: Приветствия, сообщения о выигрыше/проигрыше.
- **Поддержка**: Контакты для раздела "Помощь".
- **Ключевые слова**: Фильтрация товаров в чеках.

## 🧩 Модульная Система

Платформа поддерживает расширение функционала через модули.

### 🔌 Auto-Discovery
Чтобы добавить новый функционал, создайте файл в папке `modules/`:
- Система **автоматически найдет и загрузит** модуль при старте.
- Модуль появится в админ-панели в разделе "Модули".
- Никаких правок в `main.py` не требуется.

### � Кастомные боты

Для сложной бизнес-логики используйте папку `bots/`:

```
bots/
└── my_custom_bot/
    ├── __init__.py      # Экспортирует custom_bot инстанс
    ├── manifest.json    # Метаданные бота
    └── handlers.py      # Логика бота
```

Подробная документация: [`bots/README.md`](bots/README.md)

## 🔧 Администрирование

### Сервисы systemd
```bash
# Основной процесс ботов (Manager + Scheduler)
sudo systemctl status admin_bots

# Админ-панель (Web)
sudo systemctl status admin_panel
```

### 📋 Просмотр логов
```bash
# Логи админ-панели (в реальном времени)
sudo journalctl -u admin_panel -f

# Последние 100 строк логов панели
sudo journalctl -u admin_panel -n 100 --no-pager

# Логи ботов
sudo journalctl -u admin_bots -f

# Ошибки Nginx
sudo tail -f /var/log/nginx/error.log
```

### Резервное копирование
Автоматически создаются ежедневные бэкапы базы данных в `/var/backups/admin-bots-platform/`.
Ручной запуск: `sudo bash /opt/admin-bots-platform/scripts/backup.sh`

### 🚚 Миграция сервера
В админ-панели доступен раздел **Settings -> Миграция**, который сгенерирует пошаговую инструкцию для переезда на новый сервер с сохранением всех данных ботов.

### Переменные окружения (.env)
В файле `.env` хранятся только **системные** настройки уровня сервера:
- `DATABASE_URL`, `REDIS_URL`
- `ADMIN_PANEL_USER`, `ADMIN_PANEL_PASSWORD` (доступ в админку)
- `ADMIN_SECRET_KEY`
- `PROVERKA_CHEKA_TOKEN` (API чеков)

*Все настройки ботов (токены, тексты) хранятся в базе данных.*

---

## 📞 Поддержка

**Issues:** [GitHub Issues](https://github.com/aleksey852/adminbots/issues)

**Version:** 2.3.0 (Import/Export & Custom Bots)
**License:** MIT

