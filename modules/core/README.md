# Core Module

Базовый модуль навигации: главное меню, FAQ, помощь.

## Зависимости

Нет — это базовый модуль.

## Описание

Core обеспечивает:
- Команду `/start` с приветствием
- **Динамическое главное меню** — кнопки зависят от включённых модулей
- FAQ и контакты поддержки

## Динамическое меню

Каждый модуль может объявить кнопки через `menu_buttons`:

```python
class PromoModule(BotModule):
    menu_buttons = [
        {"text": "🔑 Ввести промокод", "order": 10}
    ]
```

Core собирает кнопки от всех включённых модулей и строит меню.

## Handlers

| Trigger | Handler | Description |
|---------|---------|-------------|
| `/start` | `start_handler` | Приветствие и главное меню |
| `ℹ️ FAQ` | `faq_handler` | Часто задаваемые вопросы |
| `🆘 Помощь` | `help_handler` | Контакты поддержки |

## Настройки

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `show_faq` | checkbox | true | Показывать кнопку FAQ |
| `show_help` | checkbox | true | Показывать кнопку Помощь |

## Сообщения

| Key | Description |
|-----|-------------|
| `welcome` | Приветственное сообщение |
| `faq_text` | Текст FAQ |
| `help_text` | Контакты поддержки |

## События

### Испускает
- `core.user_started` — `{ user_id, is_new }` — пользователь нажал /start

### Слушает
Нет
