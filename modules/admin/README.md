# Admin Module

Инструменты администратора внутри Telegram-бота.

## Зависимости

- `core` — главное меню

## Описание

Предоставляет админам (из списка ADMIN_IDS) специальные команды
для управления ботом прямо из Telegram.

## Handlers

| Trigger | Handler | Description |
|---------|---------|-------------|
| `📊 Статистика` | `admin_stats` | Общая статистика бота |
| `📢 Рассылка` | `admin_broadcast` | Начать рассылку |
| `/admin` | `admin_menu` | Меню администратора |
| `/ban {user_id}` | `ban_user` | Заблокировать пользователя |
| `/unban {user_id}` | `unban_user` | Разблокировать пользователя |

## Настройки

Нет настраиваемых параметров — использует глобальный ADMIN_IDS.

## Проверка доступа

```python
from config import ADMIN_IDS

def is_admin(telegram_id: int) -> bool:
    return telegram_id in ADMIN_IDS
```

## Сообщения

| Key | Description |
|-----|-------------|
| `stats_text` | Шаблон статистики |
| `broadcast_prompt` | Инструкция для рассылки |
| `broadcast_started` | Рассылка начата |
| `user_banned` | Пользователь заблокирован |
| `user_unbanned` | Пользователь разблокирован |
| `access_denied` | Доступ запрещён |

## События

### Испускает
- `admin.broadcast_started` — `{ admin_id, message_id }`
- `admin.user_banned` — `{ admin_id, target_user_id }`

### Слушает
Нет
